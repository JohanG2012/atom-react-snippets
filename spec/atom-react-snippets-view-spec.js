'use babel';

import AtomReactSnippetsView from '../lib/atom-react-snippets-view';

describe('AtomReactSnippetsView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
